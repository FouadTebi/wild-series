<nav class="navbar navbar-expand-lg navbar-dark">
  <div class="container-fluid">
    <a class="navbar-brand" href="#"><strong>Wild Séries</strong></a>    
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse justify-content-center" id="navbarNav">
      <ul class="navbar-nav">
        <li class="nav-item">
          <a class="nav-link text-white" href="/">Home</a>
        </li>
        <li class="nav-item">
          <a class="nav-link text-white" href="{{ path('program_index') }}">Programmes</a>
        </li>
        <li class="nav-item">
          {{ component('navbar') }}
        </li>
        <li class="nav-item">
          <a class="nav-link text-white" href="{{ path('app_season_index') }}">Saisons</a>
        </li>
        <li class="nav-item">
          <a class="nav-link text-white" href="{{ path('app_episode_index') }}">Episodes</a>
        </li>
        <li class="nav-item">
          <a class="nav-link text-white" href="{{ path('actor_index') }}">Acteurs</a>
        </li>
      </ul>
      <ul class="navbar-nav">
        {% if app.user %}
          {{ app.user.email }}
          <form action="{{ path('app_logout') }}" method="post">
            <button type="submit" class="nav-link text-white" style="background-color: transparent; border: none;">Se déconnecter</button>
          </form>
        {% else %}
          <li class="nav-item text-white">
            <a class="nav-link text-white" href="{{ path('app_security') }}">Inscription</a>
          </li>
            <a href="{{ path('app_login') }}" class="nav-link text-white">Se connecter</a>
        {% endif %}
      </ul>
    </div>
    <button {{ stimulus_action('theme', 'toggleDarkMode') }}>Toggle dark mode</button>
  </div>
</nav>